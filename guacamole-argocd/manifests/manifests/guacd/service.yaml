apiVersion: v1
kind: Service
metadata:
  name: guacd
  namespace: guacamole
  labels:
    app: guacd
    component: daemon
spec:
  selector:
    app: guacd
  ports:
  - name: guacd
    port: 4822
    targetPort: 4822
    protocol: TCP
  type: ClusterIP